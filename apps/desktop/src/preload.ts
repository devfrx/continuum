// Preload runs in an isolated context with access to Node APIs.
// Currently empty — exposes nothing to the renderer (web app uses HTTP).
// Add `contextBridge.exposeInMainWorld(...)` here later for native features.
export {};
